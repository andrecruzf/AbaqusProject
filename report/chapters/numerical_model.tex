\chapter{Automated Numerical Framework}\label{chap:numerical_model}

\section{Framework Architecture}
\subsection{Master Configuration File}

The numerical framework is controlled from a master configuration file. It
defines the test type, specimen dimensions, punch setup, mesh controls, material
orientation, loading conditions, solver resources, Euler paths, and post
processing settings. Campaign parameters can therefore be changed in one place,
while the model generation scripts remain fixed. A prototype graphical
user interface built with \software{Streamlit} drives the same configuration
and submission logic, as described in Appendix~\ref{app:interface}.

\subsection{Automated Workflow}

For each case, the framework reads the master configuration and runs the same
model generation sequence as a \software{Python} script in \software{Abaqus/CAE}.
The scripts create the geometry, material definition, contact setup, boundary
conditions, output requests, and \software{Abaqus} job, and write the input
file together with a CAE model for visual verification. The full pipeline
architecture and the corresponding model generation sequence are shown in
Appendix~\ref{app:pipeline}, Figures~\ref{fig:architecture}
and~\ref{fig:model_gen_sequence}.

\subsection{Integration with the ETH Euler Cluster}

The simulations are executed with \software{Abaqus/Explicit}~2023 on the ETH
Euler cluster. The project files are copied to the cluster, the input file and
the user material subroutine are submitted through \software{SLURM}, and the
ODB post processing runs after the analysis has completed.

Only the compact result files, figures, and movies are copied back to the
project directory. The large ODB files remain in scratch storage, which is
periodically purged, so the compact result files constitute the durable record
of each simulation.

\section{Automated Model Generation}

\subsection{Test Configurations}

Three test configurations are supported. The Nakazima model represents the
standard hemispherical punch test and is used as the main reference
configuration. The Marciniak model uses a flat punch and provides a second
forming setup with a different strain path character. The Punch in Punch model
contains an outer and an inner punch and is used to study a two stage forming
process.

All three configurations are generated by the same framework. Differences such
as the number of punches, tool geometry, contact pairs, and loading sequence
are handled through the configuration file. 

\subsection{Geometry, Contact and Boundary Conditions}

To reduce the computational cost, only one quarter of the specimen is
modelled. This restriction is possible because all simulated cases use the
\SI{0}{\degree} or \SI{90}{\degree} material orientation, for which the
material axes coincide with the specimen axes. Symmetry boundary conditions
are applied on the two cut planes, while all tooling components are
represented as analytical rigid bodies. The forming motion is prescribed
through the punch reference point, whereas the die and blank holder remain
fixed. In the experimental setup, a toothed ring is indented into the outer
radius of the blank in a preliminary clamping step, which prevents any draw in
of the flange during the test. The model represents this clamping by fully
constraining the blank along the innermost ring of the clamped outer region.

Contact between the tools and the blank is modelled through surface to surface
contact pairs with kinematic enforcement of the hard normal constraint and a
penalty regularised isotropic Coulomb friction model. The die and blank holder
contacts use a Coulomb coefficient of \num{0.35}, adopted from the previous
numerical setup of Sen~\cite{Sen2025}, while the punch to blank coefficient is
treated as a study parameter and examined in
Section~\ref{sec:friction_sensitivity}. The same contact formulation is
employed for the Nakazima and Marciniak configurations, while the Punch in
Punch model activates the additional contact interactions associated with the
second punch.

\subsection{Parameterised Mesh Generation}
\label{sec:mesh_generation}

The deformable sheet is discretised using linear hexahedral elements with
reduced integration (C3D8R). A solid formulation is adopted because the
constitutive model accounts for through thickness deformation, damage evolution,
and element deletion.

A central requirement of the framework is that the element size is controlled in
a reproducible, geometry independent way, so that simulations of different
specimen widths and punch configurations can be compared without the mesh
acting as an uncontrolled variable. Rather than meshing each model by hand, the
specimen is partitioned into a fixed set of concentric sections. These sections
establish a common meshing topology for every geometry, as shown in
Figure~\ref{fig:bm_mesh_zones} for the two specimen families. They run from a
fine zone at the pole, where the strain gradients are highest, outward to a
coarser zone at the clamped flange. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\linewidth]{img/mesh_settings_zones_clean.png}
    \caption[Partitioned meshing topology shared by all specimen geometries]{Partitioned meshing topology shared by all specimen geometries,
shown for the \texttt{W020} to \texttt{W120} family (left) and the \texttt{W200}
family (right). Each specimen
is divided into concentric sections, from the fine central zone (innermost,
blue) through the transition and dome sections outward to the coarse flange zone
(outermost, pink). The same topology is reused for every specimen width and
punch.}
    \label{fig:bm_mesh_zones}
\end{figure}

\subsubsection{Concentric Sections}

Each section is assigned a target element size, and the specimen edges within it
are seeded to reproduce that size. The target size increases from the pole
towards the flange, following the decreasing strain gradient. The central zone
is the finest and the flange zone the coarsest, with two to three intermediate sections
in between (Table~\ref{tab:mesh_zones}). Two properties follow from this
construction. First, because the same sectioning and the same target sizes are
applied to every geometry, the element size at a corresponding location is
identical across all specimen widths and punch diameters. The specimen width
only changes how far the outer sections extend. Second, the central zone keeps a
constant fine resolution, so the region where fracture initiates is always
resolved consistently. Through the thickness, ten element layers are used for
every blank, independent of the refinement factor. This count is an empirical
choice that resolves the through thickness strain gradients of a developing
neck. It is kept fixed because additional layers would act as a further mesh
refinement and would reduce the stable time increment, so the through
thickness resolution would otherwise couple directly into the computational
cost.

\begin{table}[H]
\centering
\caption{Default target element sizes of the concentric sections, from the pole
outward, for the standard specimen family.}
\label{tab:mesh_zones}
\begin{tabular}{@{}llc@{}}\toprule
Section & Region & Target element size (\si{\milli\meter}) \\ \midrule
Central & Pole/punch apex   & 0.2 \\
Transition & Below the dome    & 0.4 \\
Dome & Main stretched region   & 0.8 \\
Flange & Clamped outer zone    & 1.2 \\ \bottomrule
\end{tabular}
\end{table}

\subsubsection{Global Refinement and Independent Section Sizing}

For routine use and for mesh convergence studies, the whole mesh is refined or
coarsened with a single global refinement factor. This factor multiplies the
seed spacing of every section, while the section boundaries remain unchanged.
A factor of \num{2} therefore doubles the in plane seed spacing, which gives a
fourfold larger in plane cell area and roughly fourfold fewer in plane cells,
apart from small differences introduced by the partition geometry and the
fixed through thickness layering. Values below \num{1} refine the mesh, while
values above \num{1} coarsen it.

The target size of each section can also be chosen independently. This makes it
possible to tune how quickly the elements coarsen from the fine pole towards
the flange and to concentrate elements where they are most needed. The global
refinement factor is recovered as a particular choice within this same set of
section controls. Example meshes illustrating both controls are given in
Appendix~\ref{app:numerical_implementation}.

\section{Numerical Formulation}

\subsection{Material Model}

The sheet is described through a user material subroutine
(VUMAT)~\cite{Roth2014}. Plasticity is modelled with a Hill'48 yield function
and a non associated flow rule. The hardening law combines Swift and Voce
terms, and fracture initiation follows the Hosford and Coulomb model. The
subroutine also provides Johnson Cook type strain rate and temperature
contributions, which are inactive in the present calibration, so the
constitutive response is rate and temperature independent.

The 46 material constants were taken from an existing chair internal
calibration of AA5182, a comparable \mbox{5xxx series} aluminium alloy, since
the AA5754 batch of the campaign had not yet been characterised. The
calibration overview and the full parameter set are reproduced in
Appendix~\ref{app:material_calibration}. They are used here as a surrogate, so
the numerical results serve to verify the framework rather than to predict the
AA5754 forming limits quantitatively.

The material orientation is defined through a local material coordinate system,
whose rolling direction can be specified in the master configuration. This
allows rolling direction and transverse direction simulations to be performed
using the same numerical model without modifying the underlying implementation.

Material degradation and fracture are represented through the internal damage
variables of the VUMAT. The fracture indicator accumulates plastic strain
weighted by the Hosford and Coulomb strain to fracture at the current stress
state, and an element is deleted once the indicator reaches its critical
value. Besides
representing crack initiation, the resulting deletion pattern is used during
post processing to identify the fracture location and distinguish valid dome
centred fractures from failures occurring outside the evaluation region.

\subsection{Quasi Static Solution Strategy}

Although the experimental forming tests are quasi static, the numerical
simulations are performed using \software{Abaqus/Explicit}. Maintaining a
quasi static response therefore requires limiting the inertial effects
introduced by the explicit solution procedure while keeping the computational
cost within reasonable limits.

The numerical behaviour is governed by three coupled quantities, namely the
punch velocity, the mesh discretisation, and the applied mass scaling. A finer
mesh reduces the stable time increment and increases the computational cost,
whereas a higher punch velocity or more aggressive mass scaling reduces the
cost at the price of larger inertial effects. In the experiment the punch
advances at roughly \SI{1}{\milli\meter\per\second} over a travel on the order
of \SI{30}{\milli\meter}, so the physical forming event lasts on the order of
\SI{30}{\second}, which is prohibitive at the stable time increment of an
explicit analysis. The punch velocity is therefore fixed at
\SI{5}{\milli\meter\per\second}, following the previous numerical setup of
Sen~\cite{Sen2025} as a starting point rather than as a validation of
quasi static behaviour, which shortens the simulated process to about
\SI{6}{\second}. Because the rate and temperature terms of the material model
are inactive in the present calibration, the increased punch velocity affects
the solution only through inertia. Fixing the velocity removes one degree of
freedom, so the sensitivity study of Chapter~\ref{chap:results} varies only
the mesh density and the mass scaling, and whether the resulting simulations
are sufficiently quasi static is checked from the energy histories of each
run.

The punch motion follows a smooth step velocity profile, in which the velocity
rises gradually from zero to a peak at mid stroke and returns to zero, so that
no velocity discontinuity occurs at first punch contact. All simulations
reported in this work use this profile.

Mass scaling artificially modifies the material density so that the stable
time increment reaches a prescribed target $\Delta t_\mathrm{MS}$. The choice
of this target is the subject of the mass scaling study in
Chapter~\ref{chap:results}.

To verify the validity of each simulation, the kinetic and internal energy
histories are extracted automatically during post processing. The ratio of
kinetic to internal energy ($\mathrm{ALLKE}/\mathrm{ALLIE}$) is then evaluated
to quantify the influence of inertia. Together with the mesh and mass scaling
sensitivity studies presented in Chapter~\ref{chap:results}, this provides the
basis for selecting numerical parameters that remain representative of the
physical quasi static experiment.

\section{Automated Post Processing Outputs}

Following each simulation, the output database (ODB) is processed
automatically through a common post processing pipeline. The extracted
quantities serve two complementary purposes. They validate the numerical
simulation and generate the data required for forming limit identification.

To assess the quality and consistency of the simulation, the pipeline extracts
the punch force displacement response, the kinetic and internal energy
histories, and a series of diagnostic visualisations, including section and
isometric view animations together with representative mesh topology snapshots.
These outputs allow the quasi static assumption, deformation evolution, and
overall simulation behaviour to be verified without manual inspection of the
ODB.

For forming limit identification, element based quantities are extracted
throughout the simulation, including strain histories, damage variables, and
element status information. These data are used to detect crack initiation and
identify the fracture location. The extraction of a critical strain state from
the evaluation region is implemented, but this step remains a development output
and is not used as a reliable forming limit point in the present report.

The post processing code also includes the implemented Volk Hora logic. When
requested, the numerical strain histories are evaluated with this time dependent
necking indicator, alongside the damage evolution predicted by the constitutive
model. These outputs are useful for checking the numerical workflow and for
preparing a future comparison with the experimental evaluation. They should not
yet be interpreted as validated numerical forming limits.

This automated post processing stage converts the raw finite element results
into a standardised set of numerical outputs. It mirrors the structure of the
experimental evaluation procedure, but the numerical forming limit points remain
provisional until the post processing pipeline has been validated against a
complete numerical campaign.
